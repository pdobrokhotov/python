In Alien Invasion, the player controls a ship that appears at
the bottom center of the screen. The player can move the ship
right and left using the arrow keys and shoot bullets using the
spacebar. When the game begins, a fleet of aliens fills the sky
and moves across and down the screen. The player shoots and
destroys the aliens. If the player shoots all the aliens, a new fleet
appears that moves faster than the previous fleet. If any alien hits
the player’s ship or reaches the bottom of the screen, the player
loses a ship. If the player loses three ships, the game ends.
(see page 240 in book  https://www.booksfree.org/python-crash-course-by-eric-matthes-pdf-free-download/ )

------------------------- TECH NOTES -----------------------
This project assumes that pip-package is INSTALLED
Check it (on Windows) by the command: python -m pip --version
Commonly it ius already installed with Python3 and 
thus the comman above said it was installed already on my package:
   > pip 22.2.2 from D:\Programs\Python\lib\site-packages\pip (python 3.10)

When pip is installed you need to install "Pygame"-package
                   Installing Pygame on Windows
see https://www.codegrepper.com/code-examples/shell/install+pygame+in+python+3.10
run interminal (for Windows 10 and Python 3.10)
pip install pygame 